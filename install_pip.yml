---
- hosts: all
  name: Install Pip
  become: yes
  vars:
    ansible_python_interpreter: /usr/bin/python3
  
  tasks:
    - easy install: 
        name: pip
        state: latest
      become_user: root
