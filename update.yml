---
- hosts: all
  name: Update Machine
  
  tasks:
  - name: Run 'apt-get update'
    apt:
      update_cache: yes
  
  - name: update all apts
    apt:
      name: "*"
      state: latest
      
  
